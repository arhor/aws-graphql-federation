name: Scheduled Tasks service CI

on:
  push:
    branches:
      - 'master'
    paths:
      - '.github/workflows/app-service-scheduled-tasks-CI.yml'
      - 'app-service-scheduled-tasks/**'
      - 'lib-*/**'
  pull_request:
    branches:
      - 'master'
    paths:
      - '.github/workflows/app-service-scheduled-tasks-CI.yml'
      - 'app-service-scheduled-tasks/**'
      - 'lib-*/**'
  workflow_dispatch:

jobs:
  test_and_build:
    name: Test and Build
    uses: ./.github/workflows/build-and-verify-code-coverage.yml
    with:
      APP_SERVICE_NAME: 'app-service-scheduled-tasks'
