name: Comments service CI

on:
  push:
    branches:
      - 'master'
    paths:
      - 'app-service-comments/**'
      - 'lib-common/**'
      - 'lib-platform/**'
      - 'lib-spring-dgs/**'
      - 'lib-spring-webmvc-config/**'
      - 'lib-spring-webmvc-security/**'
      - 'lib-spring-webmvc-tracing/**'
  pull_request:
    branches:
      - 'master'
    paths:
      - 'app-service-comments/**'
      - 'lib-common/**'
      - 'lib-platform/**'
      - 'lib-spring-dgs/**'
      - 'lib-spring-webmvc-config/**'
      - 'lib-spring-webmvc-security/**'
      - 'lib-spring-webmvc-tracing/**'
  workflow_dispatch:

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'corretto'

      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2
        with:
          gradle-version: wrapper

      - name: Build Comments service
        run: 'gradle :app-service-comments:build'
