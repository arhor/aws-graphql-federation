name: Comments service CI

on:
  push:
    branches:
      - 'master'
    paths:
      - '.github/workflows/app-service-comments-CI.yml'
      - 'app-service-comments/**'
      - 'lib-*/**'
  pull_request:
    branches:
      - 'master'
    paths:
      - '.github/workflows/app-service-comments-CI.yml'
      - 'app-service-comments/**'
      - 'lib-*/**'
  workflow_dispatch:

jobs:
  test_and_build:
    name: Test and Build
    uses: ./.github/workflows/build-and-verify-code-coverage.yml
    with:
      APP_SERVICE_NAME: 'app-service-comments'
