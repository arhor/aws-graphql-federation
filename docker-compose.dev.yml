version: "3.8"

services:

  localstack:
    image: localstack/localstack:1.4.0
    ports:
      - "4566:4566"
    volumes:
      - "${LOCALSTACK_VOLUME_DIR:-./volume}:/var/lib/localstack"
      - "/var/run/docker.sock:/var/run/docker.sock"
      - "./scripts/localstack:/etc/localstack/init/ready.d/"

  dgs-users-db:
    image: postgres:12-alpine
    ports:
      - "5501:5432"
    tmpfs:
      - "/var/lib/postgresql/data"
    env_file:
      - "./dgs-users/.env"

  dgs-users-app:
    image: dgs-users-app:latest
    build:
      context: dgs-users
    depends_on:
      - dgs-users-db
      - localstack
    env_file:
      - "./dgs-users/.env"
    network_mode: host
    healthcheck:
      test: "curl --fail --silent localhost:5001/actuator/health | grep UP || exit 1"
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

  dgs-articles-db:
    image: postgres:12-alpine
    ports:
      - "5502:5432"
    tmpfs:
      - "/var/lib/postgresql/data"
    env_file:
      - "./dgs-articles/.env"

  dgs-articles-app:
    image: dgs-articles-app:latest
    build:
      context: dgs-articles
    depends_on:
      - dgs-articles-db
      - localstack
    env_file:
      - "./dgs-articles/.env"
    network_mode: host
    healthcheck:
      test: "curl --fail --silent localhost:5002/actuator/health | grep UP || exit 1"
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

  dgs-comments-db:
    image: postgres:12-alpine
    ports:
      - "5503:5432"
    tmpfs:
      - "/var/lib/postgresql/data"
    env_file:
      - "./dgs-comments/.env"

  dgs-comments-app:
    image: dgs-comments-app:latest
    build:
      context: dgs-comments
    depends_on:
      - dgs-comments-db
      - localstack
    env_file:
      - "./dgs-comments/.env"
    network_mode: host
    healthcheck:
      test: "curl --fail --silent localhost:5003/actuator/health | grep UP || exit 1"
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

  dgs-extradata-db:
    image: postgres:12-alpine
    ports:
      - "5504:5432"
    tmpfs:
      - "/var/lib/postgresql/data"
    env_file:
      - "./dgs-extradata/.env"

  dgs-extradata-app:
    image: dgs-extradata-app:latest
    build:
      context: dgs-extradata
    depends_on:
      - dgs-extradata-db
      - localstack
    network_mode: host
    healthcheck:
      test: "curl --fail --silent localhost:5004/actuator/health | grep UP || exit 1"
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s
    env_file:
      - "./dgs-extradata/.env"

  apollo-server:
    image: apollo-server:latest
    build:
      context: apollo-server
    depends_on:
      - dgs-users-app
      - dgs-articles-app
      - dgs-comments-app
      - dgs-extradata-app
    network_mode: host
